import { NoSpaceValidation } from './no-space-validation';

describe('NoSpaceValidation', () => {
  it('should create an instance', () => {
    expect(new NoSpaceValidation()).toBeTruthy();
  });
});
